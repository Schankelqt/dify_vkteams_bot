# pyrus_client.py — заглушка, ничего не загружает

import time

def upload_json_to_task(json_data: dict, file_name: str, team_id: int):
    """
    Заглушка для отключения отправки в Pyrus.
    Этот метод имитирует успешную загрузку, но ничего никуда не отправляет.
    """

    # Просто выводим в лог, что загрузка пропущена
    print(f"🚫 [SKIP] Отправка в Pyrus отключена. Файл {file_name} НЕ был отправлен.")

    # Имитируем задержку (по желанию можешь убрать)
    time.sleep(0.5)

    # Возвращаем "успешно", чтобы основная логика не ломалась
    print(f"✅ [SKIP] Загрузка {file_name} считается выполненной (mock).")